# Git
.git
.gitignore

# IDE
.idea
.vscode

# OS
.DS_Store
Thumbs.db

# Environment (passed via docker compose / runtime)
.env
.env.*
!.env.example

# Local build artifacts
app
*.db

# Frontend — excluded from Docker context; rebuilt inside container
frontend/node_modules
frontend/dist
frontend/.output
frontend/.nuxt

# Backend — webui/dist is copied from the frontend stage inside the Dockerfile
backend/internal/webui/dist

# Docs / project meta (not needed in image)
LICENSE
NOTICE
*.md
Makefile
compose.yml
